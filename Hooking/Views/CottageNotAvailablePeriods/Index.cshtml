@{
    ViewData["Title"] = "Index";

}
@{
    var cottage = (Cottage)ViewData["Cottage"];
}

<h1>Kalendar dostupnosti vikendice @cottage.Name</h1>
@section Scripts {
   <script>
        $(document).ready(function () {
            $('#calendar').fullCalendar({
                
                header:
                {
                    left: 'prev,next today',
                    center: 'title',
                    right: 'month,agendaWeek,agendaDay'
                },
                buttonText: {
                    today: 'today',
                    month: 'month',
                    week: 'week',
                    day: 'day'
                },
                
                events: function (start, end, timezone, callback) {
                    $.ajax({
                       /* url: '/CottageNotAvailablePeriods/GetCalendarData',
                        type: "GET",
                        dataType: "json",*/
                        events: '/CottageNotAvailablePeriods/GetCalendarData/'
                       /* success: function (result) {
                            
                            
                            $.each(result, function (i, data) {
                                events.push(
                                    {

                                        title: data.Title,
                                        description: data.Description,
                                        start: data.StartDate,
                                        end: data.EndDate,
                                        backgroundColor: "#9501fc",
                                        borderColor: "#fc0101",
                                        allDay: true
                                        
                                    });
                            });
                            
                            callback(events);
                        }*/
                    });
                },

                eventRender: function (event, element) {
                    element.qtip(
                        {
                            content: event.description
                        });
                },

                editable: true
                

            });
        });
       
    </script>
   
}
<div class="row">
    <div class="panel-heading">
        <div class="col-md-8  custom-heading3">
            <h3>
                <i class="fa fa-calendar"></i>
            </h3>
        </div>
    </div>
</div> 

<div class="row">
    <div class="col-xs-9 col-xs-push-2">
        <div class="box box-primary">
            <div class="box-body no-padding">
                <div id="calendar"></div>
            </div>
        </div>
    </div>
</div>

